# Note: torch and torchaudio are pre-installed in the RunPod PyTorch image
# (runpod/pytorch:2.1.0-py3.10-cuda11.8.0-devel-ubuntu22.04). Do NOT pip
# install them — pip's default index ships CPU-only wheels on Linux which
# would silently break GPU training.
#
# If running locally without GPU PyTorch already set up, install torch
# separately first following https://pytorch.org/get-started/locally/

transformers>=4.35.0
datasets>=2.14.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0,<2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
librosa>=0.10.0
soundfile>=0.12.0
